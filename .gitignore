__pycache__/
*.py[cod]
.venv/
venv/

# Credentials. .env.example is committed; the filled-in copy never is.
.env
.env.local
*.pem

# Pipeline state — the DB is derived data, rebuildable from the archive
*.db
*.db-wal
*.db-shm

# Rendered output and working files
renders/
work/
*.log

.DS_Store
.idea/
.vscode/
